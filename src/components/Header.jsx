// Import React library for building UI components
import React from 'react';

/**
 * Header component
 *
 * This is a simple functional component that renders the header of the app.
 * It contains a heading displaying the app name.
 */
const Header = () => (
  <header>
    <h1>React Task Tracker</h1> {/* App title displayed in the header */}
  </header>
);

// Export Header component to be used in other parts of the application
export default Header;
